name: Build
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Watch
        run: cat docker-compose.yml
      - name: Up
        run: docker-compose up -d